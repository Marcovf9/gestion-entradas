[build]
  base    = "frontend"
  command = "npm run build"
  publish = "dist"

[build.environment]
  NODE_VERSION = "20"

# Redirigir todo al index.html para que el SPA maneje el ruteo
[[redirects]]
  from   = "/*"
  to     = "/index.html"
  status = 200
